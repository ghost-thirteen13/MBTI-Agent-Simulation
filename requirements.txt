# ===== 核心（囚徒困境命令行实验）=====
openai              # DeepSeek API（OpenAI SDK 兼容协议）
python-dotenv       # 加载 .env 中的 DEEPSEEK_API_KEY

# ===== RAG 记忆（可选，use_rag=True 时需要）=====
pymilvus            # Milvus 向量库客户端
sentence-transformers  # 句向量编码（会连带安装 torch / transformers）
numpy

# ===== 前端可视化后端（第二阶段，api_server.py）=====
flask
flask-cors
